namespace Identificatori
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Identificatori in C#
            string nome = "Mario Rossi";

            // _ (Underscore)
            string codice_fiscale = "";

            // (A-Z, (a-z), 0-9 e _)
            string mioNome = "Mario"; 

            Console.WriteLine(mioNome); //Console.WriteLine(mionome); ERRORE


            Console.WriteLine("Hello, World!");
        }
    }
}
